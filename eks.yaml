apiVersion : eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: roboshop-dev
  region: us-west-1
managedNodeGroups:
  - name: roboshop-dev
    instanceTypes:: ["t3.micro"]
    desiredCapacity: 2
    spot: true
